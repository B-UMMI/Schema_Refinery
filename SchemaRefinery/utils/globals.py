import logging
from typing import Optional

LOGGER: Optional[logging.Logger] = None # This can change to a logger object if user uses --logger

DEBUG: bool = False # This can change to True if user uses --debug
